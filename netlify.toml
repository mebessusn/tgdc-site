# Netlify build settings. Used when you connect this project to Netlify via
# Git (auto-deploy on every change). For drag-and-drop deploys you just drop
# the already-built `dist` folder and this file isn't needed.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
